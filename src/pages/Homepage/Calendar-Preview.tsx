import React from "react";
import styled, { css } from "styled-components";
import Preview from "./Preview";

export default function Calendar_Preview() {
    return <Preview col="stripe2">
        
    </Preview>
}